{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.17",
    "@types/node": "^24.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}

{
  "scripts": {
    // 既存の scripts の下に追記
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    // ts-node を dependencies に入れておきます
    "ts-node": "^10.x"
    // （他にも express, socket.io など必要なもの）
  }
}
